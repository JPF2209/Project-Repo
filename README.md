The docker-compose file and .env are the files for the IoT data simulator

The Node-Red JSON is for running Node-Red and the other JSON file is for the IoT data simulator

The .ino code runs on the Arduino Nano 33 IoT
